services:
  mysql:
    image: mysql:8.4
    container_name: planwith-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: planwith
    ports:
      - "3306:3306"
    volumes:
      - planwith-mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-ppassword"]
      interval: 10s
      timeout: 5s
      retries: 10

  mongodb:
    image: mongo:7
    container_name: planwith-mongodb
    ports:
      - "27017:27017"
    volumes:
      - planwith-mongo-data:/data/db

  redis:
    image: redis:7-alpine
    container_name: planwith-redis
    ports:
      - "6379:6379"
    volumes:
      - planwith-redis-data:/data

  kafka:
    image: apache/kafka:3.7.1
    container_name: planwith-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    volumes:
      - planwith-kafka-data:/tmp/kraft-combined-logs

volumes:
  planwith-mysql-data:
  planwith-mongo-data:
  planwith-redis-data:
  planwith-kafka-data:
